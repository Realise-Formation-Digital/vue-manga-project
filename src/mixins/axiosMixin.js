/* const axiosMixin = {
    methods: {
        async showChosen(link) {
            try {
            const axios = require ('axios')
            this.result = await axios.get(link)
            console.log("Data fetched", this.result);
        } catch (e) {
            console.error(e)
        }
        }
    }
}

export default axiosMixin

*/