/* WORK IN PROGRESS
const axios = require('axios')

class Axios {
    static async get (url) {
        try{
            let result = null
            result = await axios.get(url)
            return result
        }catch(e){
            console.error("[Axios][get] an error has occurred when fetchin infos by the api", e)      
        }
    }
} */